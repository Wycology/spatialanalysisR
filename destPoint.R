# Calculating the coordinates of the destination point given the starting point 
# in degrees, bearing in degrees, and distance in metres

library(sp)
library(geosphere)

destPoint(p = c(35, 3), b = 60, d = 125, )